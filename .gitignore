node_modules/
data.json
.env
.env.local
*.log
.DS_Store

# Firma de la app Android. Si se filtra, cualquiera puede publicar
# actualizaciones falsas de tu app. Si se PIERDE, no podrás actualizarla nunca.
*.keystore
*.jks
casa-android/android/
casa-android/*.aab
casa-android/*.apk

# Ajustes locales de Claude Code (launch.json sí se comparte: sirve para
# arrancar el servidor con un clic).
.claude/settings.local.json
